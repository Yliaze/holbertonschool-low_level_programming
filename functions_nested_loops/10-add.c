#include "main.h"
/**
 * add - a function that adds two integers
 * and returns the result.
 *
 * @a: The input number as an integer - a.
 * @b: The input number as an integer - b.
 *
 * Return : the resultat of a + b
 */
int add(int a, int b)
{
	return (a + b);
}
